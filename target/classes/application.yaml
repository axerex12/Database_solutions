server:
  port: 8081

spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        ddl-auto: validate
  application:
    name: SpringProject
  datasource:
    url: jdbc:mariadb://localhost:3306/bigcompany
    username: admin
    password: password
